const fs=require('fs');const path=require('path');const files={cacao:'image-payloads-v3/cacao.txt',banana:'image-payloads-v3/banana.txt'};module.exports=(req,res)=>{try{const key=String(req.query&&req.query.key||'');const rel=files[key];if(!rel)return res.status(404).send('Not found');const s=fs.readFileSync(path.join(process.cwd(),rel),'utf8').trim();const data=Buffer.from(s,'base64');if(data.length<1000||data.toString('ascii',0,4)!=='RIFF'||data.toString('ascii',8,12)!=='WEBP')return res.status(500).send('Invalid image');res.setHeader('Content-Type','image/webp');res.setHeader('Content-Length',String(data.length));res.setHeader('Cache-Control','public, max-age=31536000, immutable');return res.status(200).end(data)}catch(e){return res.status(500).send('Image unavailable')}};