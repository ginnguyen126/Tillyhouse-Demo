const FILES={cacao:'image-payloads-v3/cacao.txt',banana:'image-payloads-v3/banana.txt'};const SOURCE_REF='1698f90eca39a5ec5eef2483032a1a9723e82c68';const RAW_BASE=`https://raw.githubusercontent.com/ginnguyen126/Tillyhouse-Demo/${SOURCE_REF}/`;module.exports=async(req,res)=>{try{const key=String(req.query&&req.query.key||'');const rel=FILES[key];if(!rel)return res.status(404).send('Not found');const upstream=await fetch(RAW_BASE+rel,{headers:{'User-Agent':'TillyHouse-Vercel-Image-Proxy'}});if(!upstream.ok)return res.status(502).send('Source image unavailable');const source=(await upstream.text()).replace(/\s+/g,'');const data=Buffer.from(source,'base64');if(data.length<1000||data.toString('ascii',0,4)!=='RIFF'||data.toString('ascii',8,12)!=='WEBP')return res.status(500).send('Invalid image');res.setHeader('Content-Type','image/webp');res.setHeader('Content-Length',String(data.length));res.setHeader('Cache-Control','public, max-age=86400, s-maxage=31536000, stale-while-revalidate=86400');return res.status(200).end(data)}catch(e){console.error('menu-image-v3',e);return res.status(500).send('Image unavailable')}};